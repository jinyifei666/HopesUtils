//复选框是否被选中监听 一单有变化就会执行这个方法
checkBox.setOnCheckedChangeListener(new OnCheckedChangeListener() {
		@Override
		public void onCheckedChanged(CompoundButton buttonView,
				boolean isChecked) {
				//isChecked表示复选框的状态,true表示选中
		}
	});